module_name: my_control_test

parameters:
  - group: Control Configuration
    definitions:
      MY_CONTROL_TEST_RATE:
        type: int32
        default: 10
        min: 1
        max: 200
        increment: 1
        unit: Hz
        description: |
          Controls the frequency at which the my_control_test module sends query commands.
