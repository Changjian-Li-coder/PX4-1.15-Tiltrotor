module_name: my_control

parameters:
  - group: UART Configuration
    definitions:
      MY_UART_RATE:
        type: int32
        default: 10
        min: 1
        max: 200
        increment: 1
        unit: Hz
        description: |
          Controls the frequency at which the my_uart module sends query commands.
